# Environment variables for docker-compose.yml
# Copy to .env and adjust values as needed. Never commit .env with real secrets.

DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=healthdb
DB_HOST=postgres
DB_PORT=5432

APP_PORT=8080
JAVA_OPTS=-Xms256m -Xmx512m
