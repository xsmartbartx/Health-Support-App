apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "service-a.fullname" . }}-postgres-policy
spec:
  podSelector:
    matchLabels:
      app: {{ include "service-a.name" . }}-postgres
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: {{ include "service-a.name" . }}
      ports:
        - protocol: TCP
          port: 5432
