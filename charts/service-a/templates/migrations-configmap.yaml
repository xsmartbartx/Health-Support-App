apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "service-a.fullname" . }}-migrations
data:
{{- range $path, $_ := .Files.Glob "migrations/*.sql" }}
  {{ base $path }}: |
{{ $.Files.Get $path | indent 4 }}
{{- end }}
